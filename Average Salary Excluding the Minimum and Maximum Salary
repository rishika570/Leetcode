class Solution {
    public double average(int[] salary) {
        int n=salary.length;
       Arrays.sort(salary);
        int s=0;
        double avg;
        for(int i=1;i<=n-2;i++){
            s=s+salary[i];
        }
        avg=(double)s/(n-2);
        return avg;
    }
}
